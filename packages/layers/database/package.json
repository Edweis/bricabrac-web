{
  "name": "database",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "compile": "tsc",
    "copy": "copyfiles -f ./package.json ./dist/database-layer/node_modules/database",
    "build": "npm run compile && npm run copy"
  },
  "dependencies": {
    "knex": "^0.21.5"
  },
  "devDependencies": {
    "copyfiles": "^2.3.0"
  }
}
